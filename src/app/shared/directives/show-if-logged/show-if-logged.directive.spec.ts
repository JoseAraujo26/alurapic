import { ShowIfLoggedDirective } from './show-if-logged.directive';

describe('ShowIfLoggedDirective', () => {
  it('should create an instance', () => {
    // @ts-ignore
    const directive = new ShowIfLoggedDirective();
    expect(directive).toBeTruthy();
  });
});
